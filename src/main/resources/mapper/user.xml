<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.ssafy.user.model.mapper.UserMapper">
	<select id="userLogin" parameterType="map" resultMap="userDto">
		select user_auth_id, user_name
		from users
		where user_auth_id = #{user_auth_id} and user_password = #{user_password}
	</select>
</mapper>